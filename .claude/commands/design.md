---
description: Bootstrap the JvM CI 2026 design system into a project
argument-hint: "[path to project root, defaults to ./]"
---

Invoke the jvm-skills:jvm-design skill.

Copy DESIGN.md, brand assets, and fonts into the target project root so agents
can auto-discover the Jung von Matt design system.
